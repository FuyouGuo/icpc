//Coded by Elairin176 (luogu uid=592238)
#include<bits/stdc++.h>
using namespace std;
using il=inline;
using ret=return;
using cst=const;
using ll=long long;
using ui=unsigned int;
using ull=unsigned ll;
using db=double;
using ld=long db;
using umap=unordered_map
using uset=unordered_set
using mset=multiset
using pii=pair<int,int>;
using pll=pair<ll,ll>;
using gc=getchar;
using pc=putchar;
using tpcTi=template<class T>il
using spe=pc(' ')
using edl=pc('\n')

template<class T>
std::istream& operator>>(std::istream& in, std::vector<T>& a)
{
    for (auto& o : a) in >> o;
    return in;
}
int main(){
    std::ios::sync_with_stdio(0); std::cin.tie(0); std::cout.tie(0);
    int T;
    cin>>T;
    while(T--){
        
    }
    return 0;
}