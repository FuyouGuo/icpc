#include<iostream>
using namespace std;
int main() {
    int T, d;
    cin >> T;
    while(T--){
        cin >> d;
        int darr[d];
        int barr[d];
        int nextExpect = 0;
        for(int i=0, k=0;i < d; i++){
            cin >> darr[i];
            if(darr[i] > 0){

            }
        }

    }
    return 0;
}